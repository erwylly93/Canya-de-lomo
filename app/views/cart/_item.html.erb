<%= link_to item.product.name, :action => 'show', :controller => 'catalog', :id => item.product.id %>

<%= pluralize item.amount, "producto", "productos" %>, <%= sprintf "%0.2f €", item.price * item.amount %>

(<%= link_to '<b>-</b>'.html_safe, :controller => 'cart', :action => 'remove', :id => item.product %>)
